codecov:
  require_ci_to_pass: yes

comment:
  layout: "reach, diff, flags, files"

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 60%
        threshold: 1%
